apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "photo-critique.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "photo-critique.labels" . | nindent 4 }}
data:
  # Add non‑secret configuration here
  LOG_LEVEL: "info"